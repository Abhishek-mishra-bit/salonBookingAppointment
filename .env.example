# Local Database configuration (MySQL)
DB_NAME=salon_booking
DB_USER=root
DB_PASSWORD=yourpassword
DB_HOST=localhost

# JWT configuration
JWT_SECRET=your_jwt_secret_key
JWT_EXPIRES_IN=24h

# Server port
PORT=3000

# Email configuration (if using nodemailer)
EMAIL_USER=your_email@gmail.com
EMAIL_PASS=your_app_password

# For Render PostgreSQL (use External Database URL from Render)
# DATABASE_URL=postgres://salon_booking_user:your_password@dpg-d0g7qv2dbo4c73b3vm9g-a:5432/salon_booking
